spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: 'jdbc:h2:mem:test_mem'
  kafka:
    consumer:
      group-id: spring-kafka-playground-v8
    bootstrap-servers: localhost:9092
  h2:
    console:
      enabled: true
      path: /h2-console